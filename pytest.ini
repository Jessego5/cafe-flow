[pytest]
testpaths = tests
# A generator that cannot be closed shows up here and nowhere else: the harness
# closes every unfinished order when the day is cut off at closing time.
filterwarnings =
    error::pytest.PytestUnraisableExceptionWarning
