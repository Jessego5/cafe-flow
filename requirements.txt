# What the container runs. Kept to the app so the image stays small; the
# simulator and analysis stack lives in requirements-dev.txt.
pydantic>=2.7
pyyaml>=6.0
fastapi>=0.111
sqlmodel>=0.0.18
sse-starlette>=2.1
uvicorn[standard]>=0.30
